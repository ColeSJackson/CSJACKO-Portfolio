# Dependencies and build output. Regenerated by npm install / npm run build;
# the deploy platform runs both itself, so neither belongs in the repo.
node_modules/
dist/
.astro/

# Raw camera and design exports. The site only ever reads the resized copies
# already committed under src/assets/ (see npm run photos). These are large
# (hundreds of MB) and kept local only.
/Photography 100/
/Email Design/
/Paid social assets/
/Reels Editing Work/

# OS junk
.DS_Store

# Env
.env
.env.*
!.env.example
